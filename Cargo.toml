[package]
name = "notify"
version = "0.2.0"
edition = "2024"

[dependencies]
eyre = "0.6"
color-eyre = "0.6"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
